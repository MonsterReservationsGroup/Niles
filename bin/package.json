{
  "name": "my-webpack-project",
  "version": "1.0.0",
  "bin": {
    "niles": "./bin/main.js"
  },
  "description": "My webpack project",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "webpack --mode=production --node-env=production && node PostProcessing.js && yarn copy",
    "copy": "cp -R ./package.json ./bin/package.json",
    "build:dev": "webpack --mode=development",
    "build:prod": "webpack --mode=production --node-env=production",
    "watch": "webpack --watch",
    "build:test": "cd .. && yarn build && cd bin && node main.js create"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/jest": "^27.0.2",
    "jest": "^27.3.1",
    "ts-jest": "^27.0.7",
    "ts-loader": "^9.2.6",
    "typescript": "^4.4.4",
    "webpack": "^5.62.1",
    "webpack-cli": "^4.9.1"
  },
  "dependencies": {
    "@types/inquirer": "^8.1.3",
    "inquirer": "^8.2.0"
  }
}
